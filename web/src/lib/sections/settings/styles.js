// Shared field chrome for the Settings groups. Kept in one place so every
// group's inputs line up (they used to drift between copies).

export const inputClass =
  'w-full rounded-md border-2 border-sand-300 bg-white px-3 py-2.5 font-body text-[15px] font-bold text-cocoa-900 outline-none focus:border-coral-400';

export const labelClass =
  'mb-1 block font-body text-[12px] font-extrabold uppercase tracking-wide text-cocoa-500';

/** Muted explainer line under a group's controls. */
export const hintClass = 'font-body text-[12.5px] font-bold text-text-muted';
